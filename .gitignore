/target
# Cargo.lock is committed: this workspace produces installable binaries
# (ccm-cli, ccm-mcp-server), not just libraries — reproducible builds matter.
.claude-index/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
.env.*

# Local-only: MANUAL.md is the editable source for manual/index.html (the
# published reference), kept out of git to avoid two copies drifting.
MANUAL.md
# Local-only: records this machine's absolute --root path, not portable
# to another contributor's checkout location.
.mcp.json
