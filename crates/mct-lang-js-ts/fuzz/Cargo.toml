# Standalone workspace (empty [workspace] table below): keeps this fuzzing
# harness out of the parent workspace's `members`/lockfile — it's a throwaway
# tool, not a deliverable crate.
[workspace]

[package]
name = "mct-lang-js-ts-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"

[dependencies.mct-core]
path = "../../mct-core"

[dependencies.mct-lang-js-ts]
path = ".."

[[bin]]
name = "parse-js-ts"
path = "fuzz_targets/parse_js_ts.rs"
test = false
doc = false
bench = false
